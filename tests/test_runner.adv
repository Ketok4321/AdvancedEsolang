import builtin/boolean
import builtin/type_manager
import std/list

class TestRunner extends Program:
    field input
    field output

    field errored

    method main():
        this.input = Input
        this.output = Output

        (this.input).program = this
        (this.output).program = this

        TypeManagerTester.run(this)
        BooleanTester.run(this)
        ListTester.run(this)

        if this.errored:
            (this.output).write("Some tests failed")
        end
    end
end